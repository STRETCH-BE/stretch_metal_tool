/**
 * guide content (PL) — STUB, extended by the guide build step. The EN mirror
 * is /content/en/guide.ts and must keep the same keys (parity test).
 * File path: /content/guide.ts
 */

export type GuideContent = {
  title: string;
};

export const guide: GuideContent = {
  title: "guide",
};
