/**
 * quote content (PL) — STUB, extended by the quote build step. The EN mirror
 * is /content/en/quote.ts and must keep the same keys (parity test).
 * File path: /content/quote.ts
 */

export type QuoteContent = {
  title: string;
};

export const quote: QuoteContent = {
  title: "quote",
};
