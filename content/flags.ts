/**
 * flags content (PL) — STUB, extended by the flags build step. The EN mirror
 * is /content/en/flags.ts and must keep the same keys (parity test).
 * File path: /content/flags.ts
 */

export type FlagsContent = {
  title: string;
};

export const flags: FlagsContent = {
  title: "flags",
};
