/**
 * viewer content (PL) — STUB, extended by the viewer build step. The EN mirror
 * is /content/en/viewer.ts and must keep the same keys (parity test).
 * File path: /content/viewer.ts
 */

export type ViewerContent = {
  title: string;
};

export const viewer: ViewerContent = {
  title: "viewer",
};
