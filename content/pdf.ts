/**
 * pdf content (PL) — STUB, extended by the pdf build step. The EN mirror
 * is /content/en/pdf.ts and must keep the same keys (parity test).
 * File path: /content/pdf.ts
 */

export type PdfContent = {
  title: string;
};

export const pdf: PdfContent = {
  title: "pdf",
};
