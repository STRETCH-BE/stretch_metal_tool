/**
 * admin content (PL) — STUB, extended by the admin build step. The EN mirror
 * is /content/en/admin.ts and must keep the same keys (parity test).
 * File path: /content/admin.ts
 */

export type AdminContent = {
  title: string;
};

export const admin: AdminContent = {
  title: "admin",
};
