/**
 * admin content (EN) — mirror of /content/admin.ts.
 * File path: /content/en/admin.ts
 */

import type { AdminContent } from "@/content/admin";

export const admin: AdminContent = {
  title: "admin",
};
